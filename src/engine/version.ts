/**
 * The Tamperlens engine version this MCP build reports to clients.
 *
 * Mirrored from the canonical engine (a single constant is all this client
 * needs); the full version history lives with the engine, not here.
 */
export const ENGINE_VERSION = "1.35.0";
